{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. All fs/process work stays in Rust; the webview only gets core IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
